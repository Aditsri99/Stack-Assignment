char* reverse(char *S, int len)
{
    // Using two pointer approach
    
    int i = 0;
    int j = len-1;
    
    while(j > i)
    {
        char temp = S[i];
        S[i] = S[j];
        S[j] = temp;
        i++;
        j--;
    }
    
    return S;
}
